name: release

on:
  workflow_dispatch:
  push:
    tags:
      - v*

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - run: touch hi.txt

    - name: create release
      run: gh release create ${{ github.ref }} hi.txt

